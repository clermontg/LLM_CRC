# Running LLMs a at scale on CRC
How to initiate LLM runs on CRC is specified in the word document

The other two files do run Ollama, but connect to a local instance of Ollama, not the CRC instance. I thought I had completed that loop, but it was my student. I did not have time to do it for you, but it should be simple enough. 
